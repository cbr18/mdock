import { PageHeader } from '../components/layout/PageHeader.jsx';

export function VaultPage({ slug }) {
  return (
    <section className="placeholder-page">
      <PageHeader title={slug} />
    </section>
  );
}
